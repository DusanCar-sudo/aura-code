# Dependencies & build output
node_modules/
dist/
build/

# Environment & secrets
.env
.env.local
.env.*.local
.env!.example
.aura.json

# OS / desktop artifacts
.DS_Store
.directory

# Editors
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log

# Session caches (default location; configurable via RUBY_SESSION_DIR)
.rubycode/
.rubycode-cache/

# Runtime state
.aura/
.vercel

# Test coverage
coverage/
*.lcov

# Build artifacts
*.tsbuildinfo

# Python artifacts (this is a TypeScript project)
.venv/
__pycache__/
*.pyc

# Unrelated local projects & scratch files (kept on disk, not in this repo)
deploy/
hong-thuan-videos/
*.zip
write-part1b.py
gen-pdf.js
patch-installed-registry.mjs

# Self-referential dev symlink (dev convenience, not source)
/aura-code

# Test-run artifact (generated during vitest runs)
/Aura.mp3

# Runtime-generated memory & viz state (machine output, not source)
dreams/
graphify-out/

# Downloaded tooling (reinstallable — must never be committed)
google-cloud-sdk/
google-cloud-cli-*.tar.gz

# Video render output (frames, clips and audio regenerated by the pipeline)
aura-analysis-video/quantum-computation-video/
aura-analysis-video/aura-30s-teaser/
hyperframes/output/
hyperframes/__pycache__/

# Video production projects (media, not CLI source — kept on disk, not in repo)
aura-android-video/
aura-droid-video/

# Benchmark dumps (machine output from tier runs)
.bench_tier*.json

# Local junk
*.bak
Empty File
