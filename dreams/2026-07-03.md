# Dream — 2026-07-03

> 38 episodes recalled since beginning · 35 approved · 0 ruby wins
> Categories: other

## Lessons
- [testing] A well-written test file that documents expected behavior turns off-by-one errors into fast, reliable fixes.
- [security] SQL injection vulnerabilities are eliminated by parameterizing queries, not by escaping user input.
- [security] HTML sanitizers must strip dangerous tags and event handlers (e.g., `onerror`), not just encode characters.
- [testing] Mock helpers that mutate global state (like `fetch`) must reset that state between tests to prevent leakage.
- [caching] An LRU cache eviction bug is fixed by updating the access order inside both `get` and `put` to track true recency.
- [rate-limiting] Sliding-window rate limiters require precise boundary arithmetic; allowing even one extra request is a correctness failure.
- [ci] A Dockerfile validator is incomplete without checks for pinned base‑image versions and accidental secret leakage via environment variables.
- [code-reading] Adapter files (e.g., `anthropic_adapter.py` and the transport layer) reveal the interface between an agent and an external API, making them a key target for integration work.

## Patterns
- Test‑driven bugfixing dominates: tasks include a known‑good test file, and the fix consists of aligning implementation to that test.
- Many episodes repeat the same fix across different model tiers, suggesting benchmarking or reliability regression testing.
- All episodes are short‑lived, single‑file fixes for common vulnerability or algorithmic error classes (off‑by‑one, LRU eviction, mock leakage, XSS, SQLi, rate limiting).
- No tasks were rejected; every episode resulted in an approved outcome, showing the current prompt‑and‑fix flow works reliably for these problem shapes.

## Open threads
- none

## Tomorrow brief
Expect more rapid, test‑informed bugfixes in critical code paths (security, infrastructure, caching). The last task involved reading Anthropic adapter code; next session may shift toward adapter integration or modifications. Be ready to apply the same pattern of short, targeted fixes and to potentially refactor or extend the agent’s transport layer based on today’s code reading.
